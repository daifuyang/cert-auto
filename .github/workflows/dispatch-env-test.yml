name: Dispatch Environment Test

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    environment: PRODUCTION
    steps:
      - name: Print context
        run: |
          echo "workflow_dispatch with environment works"
          echo "repo=${{ github.repository }}"
