name: Dispatch Smoke Test

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print context
        run: |
          echo "workflow_dispatch is working"
          echo "repo=${{ github.repository }}"
          echo "ref=${{ github.ref }}"
